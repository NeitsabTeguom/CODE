/* Copy to wifi_creds.h next to your build and fill in. wifi_creds.h is gitignored
 * — do NOT commit real WiFi credentials. wifi_drive.c includes it (compile-time
 * creds, the B3 plan). Field re-config over serial/SoftAP comes later. */
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
