# Changelog

The canonical changelog lives at the project root —
[`/CHANGELOG.md`](../../CHANGELOG.md).

This file used to mirror the root-level changelog up to v0.8.0,
but the duplication kept drifting. It now redirects so there is a
single source of truth (release notes are also surfaced on the
[GitHub Releases page](https://github.com/amalgame-lang/Amalgame/releases)).

