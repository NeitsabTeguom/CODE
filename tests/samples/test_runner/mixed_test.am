// Sample test file with deliberate FAIL and SKIP tags so the
// runner's counting + dispatch logic is exercised end-to-end.
// Used by tests/run_tests.sh to grep "PASS: 1  FAIL: 1  SKIP: 1"
// from amc test's summary line.

class Program {
    public static void Main() {
        Console.WriteLine("[PASS] sanity: hello")

        // Deliberate failure — runner must count it.
        Console.WriteLine("[FAIL] todo: not implemented yet")

        // Skipped on purpose.
        Console.WriteLine("[SKIP] flaky: see issue #N")
    }
}
